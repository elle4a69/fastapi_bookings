# Integrations

FastAPI Bookings is the only application runtime. Its native SMS module owns
messaging, conversations, AI orchestration, arrivals, settings, and booking
operations. Do not add a copied Assistant UI runtime here.

The current Assistant UI GitHub project may be inspected as a read-only donor
when a specific component or behaviour needs to be ported into the native SMS
module.
