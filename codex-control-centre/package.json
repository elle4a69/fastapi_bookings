{
  "name": "codex-control-centre",
  "version": "0.1.0",
  "private": true,
  "description": "Codex Control Centre - Root Quality & Verification Scripts",
  "scripts": {
    "test": "npm run test --prefix frontend && python -m pytest tests/ -v",
    "test:backend": "python -m pytest tests/ -v",
    "test:frontend": "npm test --prefix frontend",
    "lint": "python -m ruff check backend tests && npm run lint --prefix frontend",
    "lint:backend": "python -m ruff check backend tests",
    "lint:frontend": "npm run lint --prefix frontend",
    "typecheck": "python -m mypy backend && npm run typecheck --prefix frontend",
    "typecheck:backend": "python -m mypy backend",
    "typecheck:frontend": "npm run typecheck --prefix frontend",
    "build": "npm run build --prefix frontend",
    "audit": "python scripts/audit_dependencies.py && npm audit --package-lock-only --prefix frontend",
    "verify": "powershell -ExecutionPolicy Bypass -File ./test_all.ps1"
  }
}
