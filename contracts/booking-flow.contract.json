{
  "version": "2.0",
  "publicFlow": [
    {
      "step": 1,
      "name": "Auth/bootstrap",
      "calls": [
        "POST /api/public/auth/token",
        "GET /api/public/booking-forms/{slug}"
      ]
    },
    {
      "step": 2,
      "name": "Resolve configured modules",
      "allowedEntryPoints": [
        "location",
        "provider",
        "category",
        "service",
        "date_time"
      ],
      "rule": "Render only visible_modules and POST selections after each change; predefined, inferred, automatic, and disabled modules remain hidden."
    },
    {
      "step": 3,
      "name": "Search slots",
      "preferredCall": "POST /api/public/booking-forms/{slug}/availability",
      "fallbackCall": "Legacy availability endpoints are deprecated for new widgets"
    },
    {
      "step": 4,
      "name": "Select server-resolved slot",
      "call": "Use one slot returned by the form availability endpoint",
      "rule": "Automatic and Anyone provider selection is assigned by the backend."
    },
    {
      "step": 5,
      "name": "Collect client",
      "requiredFields": [
        "name or email or phone"
      ],
      "rule": "Confirming a hold without existing client_id requires client_details."
    },
    {
      "step": 6,
      "name": "Create booking",
      "calls": [
        "POST /api/public/booking-forms/{slug}/bookings"
      ],
      "result": "Booking status pending"
    },
    {
      "step": 7,
      "name": "Approval",
      "adminCalls": [
        "POST /api/admin/bookings/{id}/confirm",
        "POST /api/admin/bookings/{id}/cancel"
      ],
      "publicMessage": "Awaiting confirmation"
    }
  ],
  "adminApprovalFlow": [
    "Admin opens pending bookings from /api/admin/dashboard/bootstrap or /api/admin/bookings?status_filter=pending.",
    "Admin reviews booking details.",
    "Admin confirms with POST /api/admin/bookings/{booking_id}/confirm or cancels with POST /api/admin/bookings/{booking_id}/cancel.",
    "UI updates booking status and optionally sends notification when notifications are implemented."
  ],
  "stateMachine": {
    "pending": [
      "confirmed",
      "cancelled",
      "rescheduled"
    ],
    "confirmed": [
      "cancelled",
      "completed",
      "no_show",
      "rescheduled"
    ],
    "rescheduled": [
      "confirmed",
      "cancelled"
    ],
    "cancelled": [],
    "completed": [],
    "no_show": []
  }
}
